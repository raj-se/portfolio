# Gmail address the contact form will send FROM (needs a 16-char Google "App Password", not your normal password)
EMAIL_USER=xxxx
EMAIL_PASS=xxxxxxxxxxxxxxxx

# The inbox that should RECEIVE messages (can be the same as EMAIL_USER)
TO_EMAIL=xxxx
